# Backend — copy to backend/.env and fill in values

# SMTP settings for the newsletter job
# For local development, use Mailpit: https://mailpit.axllent.org/
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@wargameevents.local

# Base URL of the site embedded in newsletter emails as a deep-link CTA.
# No trailing slash.  Leave blank to omit the "View on website" button.
# Production example: https://eventboard.onrender.com
SITE_URL=

# Auth0 — required for JWT authentication
# Create a tenant at https://auth0.com, then a Single Page Application and an API resource.
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_AUDIENCE=https://eventboard-api

# Optional shared secret for /admin/* endpoints (service key for cron jobs / CLI).
# When set, callers may include the header:  X-Admin-Secret: <value>
# JWT with admin role also works; this is the non-interactive fallback.
ADMIN_SECRET=

# Render cron job only — URL of the deployed API.
# Set this on the Render cron service, not in backend/.env.
# Example: https://eventboard-api.onrender.com
# API_URL=

# Comma-separated list of allowed CORS origins (frontend URLs).
# Defaults to http://localhost:5173,http://localhost:8000 when unset.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000

# Frontend — copy to frontend/.env if you change the API URL
VITE_API_URL=http://localhost:5173

# Auth0 frontend vars — copy to frontend/.env
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your-spa-client-id
VITE_AUTH0_AUDIENCE=https://eventboard-api

# Public URL of the frontend (used in preview email banner link)
WEBSITE_URL=http://localhost:8000/
